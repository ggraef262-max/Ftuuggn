dkdkdkkdfkdlzkxndkk jc bc nf-- CLOCK7 EXECUTOR - ULTIMATE 2026
-- واجهة فضائية قوية - تشغيل عدة سكربتات

-- روابط السكربتات
local ARABIC_SCRIPT_URL = 'https://raw.githubusercontent.com/ggraef262-max/Ftuuggn/refs/heads/main/%D8%A7%D9%84%D8%A7%D8%B5%D8%AF%D8%A7%D8%B1%20%D8%AC%D8%AF%D9%8A%D8%AF%20%D9%85%D9%86%20clock7'
local ENGLISH_SCRIPT_URL = 'https://raw.githubusercontent.com/ggraef262-max/Ftuuggn/refs/heads/main/Bdokd'
local MUSIC_SCRIPT_URL = 'https://raw.githubusercontent.com/ggraef262-max/Ftuuggn/refs/heads/main/%D9%85%D9%88%D8%B3%D9%8A%D9%82%D9%8A'

repeat task.wait() until game:IsLoaded()

local Players = game:GetService("Players")
local TweenService = game:GetService("TweenService")
local LocalPlayer = Players.LocalPlayer

repeat task.wait() until LocalPlayer and LocalPlayer:FindFirstChild("PlayerGui")

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "Clock7_Ultimate_2026"
ScreenGui.Parent = LocalPlayer:WaitForChild("PlayerGui")
ScreenGui.ResetOnSpawn = false
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
ScreenGui.IgnoreGuiInset = dddfijdkdodif
-- الخلفية
local Space = Instance.new("Frame")
Space.Size = UDim2.new(eejifxkdc1, 0, 1, 0)
Space.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
Space.Parent = ScreenGui

-- النجوم
for i = 1, 500 do
    local star = Instance.new("Frame")
    star.Size = UDim2.new(0, math.random(1, 3), 0, math.random(1, 3))
    star.Position = UDim2.new(math.random(), 0, math.random(), 0)
    star.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
    star.BorderSizePixel = 0
    star.Parent = Space
end

-- الكواكب
local Planet1 = Instance.new("Frame")
Planet1.Size = UDim2.new(0, 150, 0, 150)
Planet1.Position = UDim2.new(0.1, 0, 0.7, 0)
Planet1.BackgroundColor3 = Color3.fromRGB(200, 100, 50)
Planet1.BorderSizePixel = 0
Planet1.Parent = Space

local Planet1Corner = Instance.new("UICorner")
Planet1Corner.CornerRadius = UDim.new(0, 75)
Planet1Corner.Parent = Planet1

local Planet2 = Instance.new("Frame")
Planet2.Size = UDim2.new(0, 200, 0, 200)
Planet2.Position = UDim2.new(0.8, 0, 0.6, 0)
Planet2.BackgroundColor3 = Color3.fromRGB(50, 100, 200)
Planet2.BorderSizePixel = 0
Planet2.Parent = Space

local Planet2Corner = Instance.new("UICorner")
Planet2Corner.CornerRadius = UDim.new(0, 100)
Planet2Corner.Parent = Planet2

local Planet3 = Instance.new("Frame")
Planet3.Size = UDim2.new(0, 100, 0, 100)
Planet3.Position = UDim2.new(0.4, 0, 0.2, 0)
Planet3.BackgroundColor3 = Color3.fromRGB(150, 50, 200)
Planet3.BorderSizePixel = 0
Planet3.Parent = Space

local Planet3Corner = Instance.new("UICorner")
Planet3Corner.CornerRadius = UDim.new(0, 50)
Planet3Corner.Parent = Planet3

-- الشعار
local Logo = Instance.new("TextLabel")
Logo.Size = UDim2.new(1, 0, 0.15, 0)
Logo.Position = UDim2.new(0, 0, 0.05, 0)
Logo.Text = "CLOCK7"
Logo.TextColor3 = Color3.fromRGB(255, 255, 255)
Logo.TextScaled = true
Logo.BackgroundTransparency = 1
Logo.Font = Enum.Font.GothamBlack
Logo.TextXAlignment = Enum.TextXAlignment.Center
Logo.Parent = ScreenGui

local SubLogo = Instance.new("TextLabel")
SubLogo.Size = UDim2.new(1, 0, 0.05, 0)
SubLogo.Position = UDim2.new(0, 0, 0.18, 0)
SubLogo.Text = "ULTIMATE 2026"
SubLogo.TextColor3 = Color3.fromRGB(0, 200, 255)
SubLogo.TextScaled = true
SubLogo.BackgroundTransparency = 1
SubLogo.Font = Enum.Font.GothamBold
SubLogo.TextXAlignment = Enum.TextXAlignment.Center
SubLogo.Parent = ScreenGui

-- البطاقات
local CardsFrame = Instance.new("Frame")
CardsFrame.Size = UDim2.new(0.95, 0, 0.5, 0)
CardsFrame.Position = UDim2.new(0.025, 0, 0.3, 0)
CardsFrame.BackgroundTransparency = 1
CardsFrame.Parent = ScreenGui

local function createCard(x, title, desc, color, url)
    local card = Instance.new("Frame")
    card.Size = UDim2.new(0.31, 0, 1, 0)
    card.Position = UDim2.new(x, 0, 0, 0)
    card.BackgroundColor3 = Color3.fromRGB(10, 10, 20)
    card.BorderSizePixel = 2
    card.BorderColor3 = color
    card.Parent = CardsFrame
    
    local corner = Instance.new("UICorner")
    corner.CornerRadius = UDim.new(0, 10)
    corner.Parent = card
    
    local icon = Instance.new("Frame")
    icon.Size = UDim2.new(0, 60, 0, 60)
    icon.Position = UDim2.new(0.5, -30, 0.15, 0)
    icon.BackgroundColor3 = color
    icon.Parent = card
    
    local iconCorner = Instance.new("UICorner")
    iconCorner.CornerRadius = UDim.new(0, 30)
    iconCorner.Parent = icon
    
    local titleLabel = Instance.new("TextLabel")
    titleLabel.Size = UDim2.new(1, 0, 0.15, 0)
    titleLabel.Position = UDim2.new(0, 0, 0.4, 0)
    titleLabel.Text = title
    titleLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
    titleLabel.TextScaled = true
    titleLabel.BackgroundTransparency = 1
    titleLabel.Font = Enum.Font.GothamBold
    titleLabel.Parent = card
    
    local descLabel = Instance.new("TextLabel")
    descLabel.Size = UDim2.new(0.9, 0, 0.2, 0)
    descLabel.Position = UDim2.new(0.05, 0, 0.55, 0)
    descLabel.Text = desc
    descLabel.TextColor3 = Color3.fromRGB(180, 180, 180)
    descLabel.TextScaled = true
    descLabel.BackgroundTransparency = 1
    descLabel.Font = Enum.Font.SourceSans
    descLabel.TextWrapped = true
    descLabel.Parent = card
    
    -- زر التشغيل
    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(0.7, 0, 0.15, 0)
    btn.Position = UDim2.new(0.15, 0, 0.8, 0)
    btn.BackgroundColor3 = color
    btn.Text = "تشغيل"
    btn.TextColor3 = Color3.fromRGB(255, 255, 255)
    btn.TextScaled = true
    btn.Font = Enum.Font.GothamBold
    btn.Parent = card
    
    local btnCorner = Instance.new("UICorner")
    btnCorner.CornerRadius = UDim.new(0, 5)
    btnCorner.Parent = btn
    
    -- تأثيرات الزر
    btn.MouseEnter:Connect(function()
        TweenService:Create(btn, TweenInfo.new(0.2), {
            BackgroundColor3 = Color3.fromRGB(255, 255, 255),
            TextColor3 = color
        }):Play()
    end)
    
    btn.MouseLeave:Connect(function()
        TweenService:Create(btn, TweenInfo.new(0.2), {
            BackgroundColor3 = color,
            TextColor3 = Color3.fromRGB(255, 255, 255)
        }):Play()
    end)
    
    -- وظيفة الزر - تشغيل السكربت وإخفاء الواجهة
    btn.MouseButton1Click:Connect(function()
        -- تشغيل السكربت
        pcall(function()
            loadstring(game:HttpGet(url))()
        end)
        
        -- إخفاء الواجهة بالكامل
        ScreenGui:Destroy()
    end)
end

-- إنشاء البطاقات
createCard(0.01, "ARABIC", "السكربت العربي", Color3.fromRGB(0, 150, 255), ARABIC_SCRIPT_URL)
createCard(0.345, "ENGLISH", "English Script", Color3.fromRGB(255, 150, 0), ENGLISH_SCRIPT_URL)
createCard(0.68, "MUSIC", "Music Script", Color3.fromRGB(200, 0, 255), MUSIC_SCRIPT_URL)

-- by DBlack
local Credit = Instance.new("TextLabel")
Credit.Size = UDim2.new(1, 0, 0.03, 0)
Credit.Position = UDim2.new(0, 0, 0.96, 0)
Credit.Text = "by DBlack"
Credit.TextColor3 = Color3.fromRGB(100, 100, 150)
Credit.TextScaled = true
Credit.BackgroundTransparency = 1
Credit.Font = Enum.Font.SourceSans
Credit.TextXAlignment = Enum.TextXAlignment.Center
Credit.Parent = ScreenGui
