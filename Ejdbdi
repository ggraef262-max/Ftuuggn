local CoreGui = game:GetService("CoreGui")
local TweenService = game:GetService("TweenService")

-- ألوان نحاسية
local COPPER = Color3.fromRGB(184, 115, 51)
local DARK_COPPER = Color3.fromRGB(120, 70, 20)
local LIGHT_COPPER = Color3.fromRGB(220, 160, 80)

-- واجهة خفيفة جداً وسريعة
local gui = Instance.new("ScreenGui", CoreGui)
gui.Name = "FastCopperLoader"
gui.IgnoreGuiInset = true
gui.ResetOnSpawn = false
gui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
gui.DisplayOrder = 999999

-- الحاوية الرئيسية (تظهر فوراً)
local container = Instance.new("Frame", gui)
container.Size = UDim2.fromScale(0.4, 0.25)
container.Position = UDim2.fromScale(0.5, 0.5)
container.AnchorPoint = Vector2.new(0.5, 0.5)
container.BackgroundColor3 = Color3.fromRGB(5, 5, 5)
container.BackgroundTransparency = 0
container.BorderSizePixel = 0
container.Visible = true -- تظهر على طول

-- زوايا
local corner = Instance.new("UICorner", container)
corner.CornerRadius = UDim.new(0, 10)

-- إطار نحاسي
local stroke = Instance.new("UIStroke", container)
stroke.Color = COPPER
stroke.Thickness = 2
stroke.Transparency = 0

-- العنوان
local title = Instance.new("TextLabel", container)
title.Size = UDim2.fromScale(1, 0.3)
title.Position = UDim2.fromScale(0.5, 0.2)
title.AnchorPoint = Vector2.new(0.5, 0.5)
title.BackgroundTransparency = 1
title.Text = "CLOCK 7"
title.Font = Enum.Font.GothamBlack
title.TextSize = 40
title.TextColor3 = LIGHT_COPPER
title.TextTransparency = 0
title.Visible = true

-- شريط التحميل
local progressBg = Instance.new("Frame", container)
progressBg.Size = UDim2.fromScale(0.8, 0.15)
progressBg.Position = UDim2.fromScale(0.5, 0.6)
progressBg.AnchorPoint = Vector2.new(0.5, 0.5)
progressBg.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
progressBg.BorderSizePixel = 0
progressBg.Visible = true

local progressCorner = Instance.new("UICorner", progressBg)
progressCorner.CornerRadius = UDim.new(1, 0)

local progressBar = Instance.new("Frame", progressBg)
progressBar.Size = UDim2.fromScale(0, 1)
progressBar.BackgroundColor3 = COPPER
progressBar.BorderSizePixel = 0
progressBar.Visible = true

local barCorner = Instance.new("UICorner", progressBar)
barCorner.CornerRadius = UDim.new(1, 0)

-- العداد
local percent = Instance.new("TextLabel", container)
percent.Size = UDim2.fromScale(1, 0.2)
percent.Position = UDim2.fromScale(0.5, 0.8)
percent.AnchorPoint = Vector2.new(0.5, 0.5)
percent.BackgroundTransparency = 1
percent.Text = "0%"
percent.Font = Enum.Font.GothamBold
percent.TextSize = 30
percent.TextColor3 = LIGHT_COPPER
percent.TextTransparency = 0
percent.Visible = true

-- بدون أي تأخير - نبدأ التحميل فوراً
local success, result = pcall(function()
    return game:HttpGet("https://raw.githubusercontent.com/ggraef262-max/Ftuuggn/refs/heads/main/Script")
end)

if success and result then
    -- تحديث سريع جداً
    for i = 1, 100 do
        progressBar.Size = UDim2.fromScale(i/100, 1)
        percent.Text = i .. "%"
        task.wait(0.002) -- سريع جداً
    end
    
    percent.Text = "✓"
    task.wait(0.2)
    
    -- تشغيل السكربت
    pcall(function() loadstring(result)() end)
    
    -- إخفاء سريع
    gui:Destroy()
else
    percent.Text = "✗"
    task.wait(0.5)
    gui:Destroy()
end
